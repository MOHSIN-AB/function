const numbers = [4,5,6,7,8];
let maixmum = infinity
let minimum = infinity
for(let number of numbers ){
    if (number>maximum)
    maximum=number

    if ( number<minimum)
    minimum=number 
}
console.log(maximum);
console.log(minimum);